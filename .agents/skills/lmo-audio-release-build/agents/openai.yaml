interface:
  display_name: "LMO Audio Release Build"
  short_description: "Build and verify the user-ready portable release."
  default_prompt: "Build the LMO Audio portable release and verify it is ready for ordinary users."
